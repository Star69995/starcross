{
  "name": "star-crossword-firebase",
  "private": true,
  "scripts": {
    "dev": "concurrently -n emulators,frontend -c yellow,cyan \"firebase emulators:start --only auth,firestore --project demo-star-crossword --import=./.emulator-data --export-on-exit=./.emulator-data\" \"npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
